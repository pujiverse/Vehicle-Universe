# Breadcrumb

Home › Domain › Vehicle Type › Brand › Product, with the current level as plain `ink` text (`aria-current="page"`).

Links take the domain accent when an item carries `domain`, else `accent`. Keep it sticky in the top bar on mobile.

Consumer provides: `items` (`{label, href | onClick, domain?}`); the last item is the current page.
