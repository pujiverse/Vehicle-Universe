# SearchField

Global search input for the top bar: search icon, visually hidden label, `border-strong` outline and a focus ring on the whole field.

Consumer provides: `value`, `onChange`, optional `label` and `placeholder` (default "Search types, brands, products"), and the results popover (group results by Types / Brands / Products).
