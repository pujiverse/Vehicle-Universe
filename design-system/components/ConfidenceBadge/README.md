# ConfidenceBadge

Outlined badge for a product's `data_confidence` (High / Medium / Low), focusable, with the tooltip "Low = estimate; verify before relying on it."

High → `status-positive`, Medium → `status-caution`, Low → `status-negative`. Place it once, at the foot of the Level 4 spec sheet.

Consumer provides: `level`.
